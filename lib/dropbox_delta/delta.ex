defmodule DropboxDelta.Delta do

  # def resolve(entries) when is_enum(entries), do: resolve
  #   entries
  #   |> Enum.map(fn([path, nil]) -> path)
  # end
  #
  # def resolve([path, nil]), do: path
  # def resolve([path, %{"is_dir": true}]), do: path
end
